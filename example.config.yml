iron:
  token: "t"
  project_id: "p"

# Queue to pull messages off
workqueue: myqueue
# Queue to set alerts on
workqueue_alertqueue: myqueue_alerts
# Name of worker to start processing workqueue
worker: consumer

# Setup code to run
setup: usersetup.rb
# Main worker processing code
code: usercode.rb

